name: zura_v2
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Firebase dependencies
  firebase_core: ^2.24.2
  firebase_auth: ^4.15.3
  cloud_firestore: ^4.13.6
  
  # UI dependencies
  flutter_screenutil: ^5.9.0
  glassmorphism: ^3.0.0
  
  # Storage and utilities
  shared_preferences: ^2.2.2
  http: ^1.1.0
  google_sign_in: ^7.1.1
  flutter_inappwebview: ^6.1.5
  url_launcher: ^6.3.2
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Add assets section for your movie database
  assets:
    - assets/